services:
  nexo:
    build:
      context: .
      dockerfile: Dockerfile
      target: production
    ports:
      - "${PORT:-3000}:3000"
    environment:
      NODE_ENV: production
    env_file:
      - .env
    restart: unless-stopped
