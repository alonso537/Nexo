services:
  nexo:
    build:
      context: .
      dockerfile: Dockerfile
      target: production
    ports:
      - "${PORT:-8000}:${PORT:-8000}"
    environment:
      NODE_ENV: production
    env_file:
      - .env
    restart: unless-stopped
